# 手机端适配说明

本版本新增 `src/main/resources/static/css/mobile.css`，专门处理手机竖屏访问。

已适配：

- 顶部导航在手机端自动换成两行，分区横向滑动；
- 帖子列表、分区栏、详情页、评论区单列显示；
- 发帖编辑器在手机端改为上方 Markdown、下方实时预览；
- 后台表格支持横向滑动，避免撑爆页面；
- 私信、通知、个人档案、公开主页、地图模块均改为竖屏布局；
- 图片、代码块、评论图、头像都有最大尺寸限制。

如果手机端样式没有变化，请在浏览器强制刷新，或清除缓存。
